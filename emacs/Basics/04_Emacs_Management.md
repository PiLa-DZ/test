C-g     --> Quit / Cancel
C-M-x   --> Refrech Config
C-x C-c --> Exit Emacs

C-x C-= --> Zoom in
C-x C-- --> Zoom out

C-x     --> (Global Prefix)
C-c     --> (Local  Prefix)

Commands M-x `TAB To auto comlitej`
    $>dired | C-x d --> File Manager
    $>calenter
    $>eshell --> Emacs Shell (Terminal)
    $>tetris

Major Mode (Local Mode) (Settings for One Buffer)
    $>hl-line-mode
Minor Mode (Global Mode) (Settings for All Buffer)
    $>global-hl-line-mode
